---
- name: Install network tools.
  community.general.homebrew:
    name: "{{ item }}"
    state: present
  become: false
  loop:
    - inetutils
    - whois
    - iputils
    - bind
    - traceroute
    - tcpdump
    - freerdp
    - nmap