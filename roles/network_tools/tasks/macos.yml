---
- name: Install network tools.
  homebrew:
    name: "{{ item }}"
    state: present
  become: false
  with_items:
    - net-tools
    - whois
    - iputils-ping
    - dnsutils
    - traceroute
    - tcpdump
    - rdesktop
    - nmap